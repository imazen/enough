[package]
name = "test-tokio"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
enough = { workspace = true }
enough-tokio = { workspace = true }
tokio = { version = "1.43", features = ["rt", "rt-multi-thread", "time", "macros", "sync"] }
tokio-util = { version = "0.7" }
